@model List<PhoneShop.Controllers.Seller.DataView.CustomerPurchase>
@using PhoneShop.Controllers.Seller.DataView;
@using PhoneShop.Models;
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutEmpty.cshtml";
    List<CustomerPurchase> GetDeli = ViewBag.GetDeli;

    
}
<h3>Thông tin đơn hàng</h3>

    
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Tên</th>
                <th>Hình Ảnh</th>
                <th>Số lượng</th>
                <th>Giá</th>
                <th>Giảm giá</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var i in Model)
        {
            <tr>
                <td>@i.ProductTitle</td>
                <td>132</td>
                <td>@i.PurchaseQuantity_Product</td>
                <td>@PhoneShop.Extension.Extension.ToVnd((double)i.PurchasePrice_Product)</td>
                <td>@PhoneShop.Extension.Extension.ToVnd((double)i.DiscountVoucher)</td>
            </tr>
            @foreach (var item_DeliveryProcess in GetDeli)
            {
                if(i.OrderDetail_Id == item_DeliveryProcess.OrderDetail_Id)
                {
                    <tr>
                        @if (item_DeliveryProcess.DeliveryStatus == 1)
                        {
                            <td colspan="2">Trạng thái đơn hàng: Đang xử lý</td>
                        }
                        else if (item_DeliveryProcess.DeliveryStatus == 2)
                        {
                            <td colspan="2">Trạng thái đơn hàng: Chuẩn bị hàng</td>
                        }
                        else if (item_DeliveryProcess.DeliveryStatus == 3)
                        {
                            <td colspan="2">Trạng thái đơn hàng: Đang giao hàng</td>
                        }
                        else if (item_DeliveryProcess.DeliveryStatus == 4)
                        {
                            <td colspan="2">Trạng thái đơn hàng: Đã giao hàng</td>
                        }
                        else
                        {
                            <td colspan="2">Trạng thái đơn hàng: Hủy đơn hàng</td>
                        }




                        <td colspan="2">Ngày xác nhận: @item_DeliveryProcess.DeliveryDate.ToString("dd/MM/yyyy")</td>
                    </tr>
                }
            }
           
           
        }
        </tbody>
    </table>

<hr />
<br />
<br />
<br />
<h3>Địa chỉ nhận hàng</h3>
@* @if (IsDeliveryProcess != null)
{
   
    @if (IsDeliveryProcess.DeliveryStatus == 1)
    {
        <h5 class="coler_title">Trạng thái đơn hàng: Đang xử lý</h5>
        <p>Ngày xác nhận: @IsDeliveryProcess.DeliveryDate</p>

        <p>@IsDeliveryProcess.DeliveryAddress</p>
    }
    else if (IsDeliveryProcess.DeliveryStatus == 2)
    {
        <h5 class="coler_title">Trạng thái đơn hàng: CHuẩn bị hàng</h5>
        <p>Ngày xác nhận: @IsDeliveryProcess.DeliveryDate</p>

        <p>@IsDeliveryProcess.DeliveryAddress</p>
    }
    else if (IsDeliveryProcess.DeliveryStatus == 3)
    {
        <h5 class="coler_title">Trạng thái đơn hàng: Đang giao hàng</h5>
        <p>Ngày nhận hàng: @IsDeliveryProcess.DeliveryDate</p>

        <p>@IsDeliveryProcess.DeliveryAddress</p>
    }
    else if (IsDeliveryProcess.DeliveryStatus == 4)
    {
        <h5 class="coler_title">Trạng thái đơn hàng: Đã giao hàng</h5>
        <p>Ngày xác nhận: @IsDeliveryProcess.DeliveryDate</p>

        <p>@IsDeliveryProcess.DeliveryAddress</p>
    }
    else
    {
        <h5 class="coler_title">Trạng thái đơn hàng: Hủy đơn hàng</h5>
    }
}
else
{

    <a>Đơn hàng đang xử lý</a>
} *@

