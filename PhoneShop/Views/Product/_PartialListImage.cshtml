@using PhoneShop.ModelViews

@model List<ImageProductViewModel>

 @if (Model != null && Model.Count() > 0)
{

    foreach (var item in Model)
    {
        
        <img src="~/images/Product/@item.DataName" alt="Image 13">
        
       
    
    }
}
