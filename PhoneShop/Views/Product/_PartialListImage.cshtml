@using PhoneShop.Models

@model List<ImageProduct>

 @if (Model != null && Model.Count() > 0)
{

    foreach (var item in Model)
    {
        <img src="~/images/Product/@item.DataName" alt="">
        
    
    }
}
